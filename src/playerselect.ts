class PlayerSelect extends Screen {
  private playerSelectButton: Button;
  private gameStartButton: Button;

  private drawImage() {}
  private drawPlayerSelectButton() {}
  private drawStartButton() {}
}